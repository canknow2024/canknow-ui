import PasswordLabel from './src/PasswordLabel';

/* istanbul ignore next */
PasswordLabel.install = function(Vue) {
  Vue.component(PasswordLabel.name, PasswordLabel);
};

export default PasswordLabel;
