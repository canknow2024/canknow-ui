import ContainerCloseButton from './src/ContainerCloseButton';

ContainerCloseButton.install = function(Vue) {
  Vue.component(ContainerCloseButton.name, ContainerCloseButton);
};

export default ContainerCloseButton;
